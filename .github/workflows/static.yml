name: Deploy static content to Pages
on:
  push:
    branches: ["master"]

  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Package Helm Charts
        run: |
          for d in ./charts/*/ ; do
            echo "Building and packaging chart in directory: $d"
            helm dependency build "$d"
            helm package "$d" -d packaged_charts
          done
      
      - name: Index charts
        run: helm repo index . --url https://sul-dlss.github.io/folio-k8s/
      
      - name: Add and Commit packaged charts and index
        run: |
          git add packaged_charts
          git add index.yaml
          git commit -m "Adding packaged charts and updated index"
      
      - name: Push packaged charts and index
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: master

      - name: Setup Pages
        uses: actions/configure-pages@v5
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
